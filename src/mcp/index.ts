/**
 * MCP (Model Context Protocol) integration entry-point.
 *
 * @example
 * ```ts
 * import { createRmsMcpServer, startMcpServer } from "@farukada/langchain-ts-rms/mcp";
 * ```
 *
 * @module
 */
export { createRmsMcpServer, startMcpServer, type RmsMcpServerOptions } from "./server.js";
